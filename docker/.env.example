# PostgreSQL
POSTGRES_USER=iliketohave
POSTGRES_PASSWORD=iliketohave_password
POSTGRES_DB=iliketohave

# Full SQLAlchemy database URL (used by backend and Alembic)
DATABASE_URL=postgresql+asyncpg://iliketohave:iliketohave_password@db:5432/iliketohave

# Exposed host ports (can be changed per project on a multi-project VPS)
DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000

# JWT
JWT_SECRET=change_me_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MIN=60
